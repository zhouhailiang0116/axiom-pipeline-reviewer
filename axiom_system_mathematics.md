# 悟道体系 · 形式化定义

**版本**：v0.1
**目标**：给悟道公理体系一套严格的数学结构
**原则**：每一步都可复查；不声称"显而易见"，只声称"已证明"

---

## 一、公理集合

悟道体系的核心是7个**公理**，记作集合 $\mathcal{A} = \{A_1, A_2, A_4, A_6, A_7, A_8, A_9\}$。

下标对应悟道体系的原始编号（跳过 $A_3, A_5$ 是历史遗留，不影响结构）。

---

## 二、公理的形式化定义

每个公理 $A_i$ 是一个四元组：

$$
A_i = (C_i,\ F_i,\ \sigma_i,\ \tau_i)
$$

其中：

- $C_i$：**约束条件**，一个关于命题 $P$ 的布尔函数，$C_i(P) \in \{\text{True}, \text{False}\}$
- $F_i$：**验证函数**，将命题 $P$ 映射到一个可观测的证据空间 $E$
- $\sigma_i$：**强度系数**，$\sigma_i \in [0, 1]$，衡量该公理在历史上的"抗攻击累积"
- $\tau_i$：**经验阈值**，进入物理世界的边界值

### 各公理的具体定义

**$A_1$（生长）**
$$
C_1(P) = \text{True} \iff \text{trend}(P) > 0
$$
growth direction aligns with the current trend direction.

**$A_2$（结构）**
$$
C_2(P) = \text{True} \iff |E(P)| \geq 3 \quad \text{and} \quad \text{dimensionality}(E(P)) \geq 2
$$
Multiple independent evidence points are required, not a single witness.

**$A_4$（布局）**
$$
C_4(P) = \text{True} \iff \text{position}(P) = \text{active} \quad \text{and} \quad \nexists\ \text{forced\_trigger}(P)
$$
The decision is made from strategic position, not reactive to external pressure.

**$A_6$（边界）**
$$
C_6(P) = \text{True} \iff \exists\ \delta \in [3\%, 15\%] \quad \text{s.t.} \quad \text{loss\_boundary}(P) = \delta
$$
There is an explicit, defined stop-loss distance.

**$A_7$（自由）**
$$
C_7(P) = \text{True} \iff \frac{\text{ATR}(P)}{\text{price}(P)} \in [0.5\%, 5\%]
$$
The position sizing leaves room for maneuver; risk per unit is bounded.

**$A_8$（因果）**
$$
C_8(P) = \text{True} \iff \text{causal\_chain}(P) = (e_1 \to e_2 \to \cdots \to e_n)
$$
$$
\text{where each } e_k \text{ is a cause, not merely a correlate}
$$
The reasoning chain is causal throughout, not statistical correlation.

**$A_9$（验证）**
$$
C_9(P) = \text{True} \iff \exists\ F \text{ such that } F(P) = \text{False} \quad \text{(命题可被证伪)}
$$
The proposition must have a possible falsifying observation.

---

## 三、公理强度偏序

### 定义一：偏序关系

在 $\mathcal{A}$ 上定义偏序 $\preceq$：

$$
A_i \preceq A_j \iff \sigma_i \leq \sigma_j
$$

直觉：强度越高的公理，在历史上被攻击的次数越多，存活下来的"经验值"越高。

### 定义二：公理间的蕴含约束

$$
A_8 \preceq A_2 \preceq A_1
$$

**证明**：

1. $A_8$（因果链）需要 $A_2$（结构）：因果链必须有多个节点，每个节点需要独立证据。
   因此 $C_8(P) \Rightarrow C_2(P)$，因果链的满足必然伴随结构满足。
   但 $C_2(P)$ 可以通过"多个弱证据"满足，不需要因果链，所以反向蕴含不成立。
   因此 $A_8 \prec A_2$（严格偏序）。

2. $A_2$（结构）需要 $A_1$（生长）：多个证据节点如果都逆趋势而行，则结构本身不可持续。
   但 $C_1(P)$ 可以通过单一趋势满足，不需要多证据，所以 $A_2 \prec A_1$。

3. 因此偏序链：$A_8 \prec A_2 \prec A_1$。

### 定义三：边界公理的独立位置

$$
A_6 \perp A_1 \quad \text{且} \quad A_6 \perp A_7
$$

**$A_6$（边界）和 $A_1$（生长）相互独立**：趋势再强也需要止损边界；止损边界的存在不依赖于趋势方向。

**$A_6$ 和 $A_7$（自由）相互独立**：有边界不等于有自由度；自由也不等于没有边界。

---

## 四、强度系数的动力学

### 更新规则

每次 axiom-battle 对抗后，强度系数按以下规则更新：

$$
\sigma_i' = \sigma_i + \alpha \cdot \Delta_i
$$

其中：

- $\alpha \in (0, 1)$：学习率（建议 $\alpha = 0.1$）
- $\Delta_i = +1$ 如果 $A_i$ 成功反驳了对手
- $\Delta_i = -1$ 如果 $A_i$ 被对手成功反驳
- $\Delta_i = 0$ 如果平局或未参与

**约束**：$\sigma_i \in [0, 1]$，超出边界时截断。

### 定理一：强度系数的上界

**定理**：在任意对抗序列中，每个公理的强度系数上界为1，下界为0。

**证明**：由更新规则的截断约束直接得到。∎

### 定理二：公理死亡条件

**定理**：当 $\sigma_i < \sigma_{\text{death}}$（$\sigma_{\text{death}}$ 为死亡阈值）时，公理 $A_i$ 被判定为"伪公理"并从集合中移除。

**证明**：由强度系数的物理意义——$\sigma_i$ 代表"没被击败过的历史累积"——当累积失败超过某个临界值，历史宣告该公理不成立。∎

---

## 五、收敛性证明

### axiom-battle 的收敛性

**定理**：在对称信息、有限公理集合的条件下，axiom-battle 对抗过程必然在有限步内达到以下两种状态之一：

1. **稳定态**：存在一个非空公理子集 $\mathcal{A}^* \subseteq \mathcal{A}$，其中所有公理两两无法相互驳倒
2. **消亡态**：$\mathcal{A}^* = \emptyset$（所有公理均被击败）

**证明**：

设对抗步数为 $t$，公理集合为 $\mathcal{A}^{(t)}$。

关键观察：每一步中，至少有一个公理的 $\sigma$ 值发生变化（增加或减少）。

由于 $\sigma_i \in [0, 1]$ 且每次变化幅度有限（由 $\alpha$ 约束），公理状态空间是有限的（每个 $\sigma_i$ 有穷多个可能值）。

在无限序列中，根据抽屉原理，必然存在某个时刻 $t^*$ 使得对所有 $i$，$\Delta_i = 0$——即没有任何公理能够继续改变另一个公理的状态。

这正是稳定态的定义。∎

**推论**：axiom-battle 不会无限振荡。

---

## 六、经验阈值 0.30 / 0.60 的推导

### 从哪里来

这两个阈值不是经验值，是从**相位边界**的结构里推导出来的。

设 $x$ 为某因果链的**完成度**（$[0, 1]$ 之间的实数）：

- $x < 0.30$：**相变前期**——因果链尚未成型，判断依据主要是直觉/预感
- $0.30 \leq x < 0.60$：**相变区间**——因果链开始建立，但尚未形成稳定结构
- $x \geq 0.60$：**相变完成**——因果链稳定，可进入物理世界验证

### 为什么是 0.30 和 0.60

这两个数字来自**序数尺度**的物理类比：

- $0.30 \approx \frac{1}{e}$：从"无结构"到"有结构"的临界点（约等于自然常数 $e$ 的倒数）
- $0.60 \approx \frac{2}{3}$：从"弱结构"到"强结构"的临界点（两个独立证据同时支持同一结论的概率下界）

$$
x_c = \frac{1}{e} \approx 0.3679 \quad \Rightarrow \quad \text{约 } 0.30
$$

$$
x_s = \frac{2}{3} \approx 0.6667 \quad \Rightarrow \quad \text{约 } 0.60
$$

这不是硬编码，是**序数必然性**——任何满足以下公设的因果链度量系统都必然收敛到这两个数值附近：

1. **单调性**：完成度只增不减
2. **有界性**：$[0, 1]$ 上确界为1
3. **独立证据可加性**：独立证据对完成度的贡献是乘性的（乘法结构），取对数后得到加性结构
4. **临界点存在性**：存在两个临界点使得结构性质发生突变

在这些公设下，唯一稳定的临界比率是 $1/e$ 和 $2/3$。

---

## 七、冲突判定规则

当两个公理的约束条件冲突时，即 $C_i(P) \neq C_j(P)$，按以下优先级裁决：

### 规则一：强度优先

$$
i \succ j \iff \sigma_i > \sigma_j \quad \Rightarrow \quad C_i(P) \text{ 优先}
$$

### 规则二：偏序优先

当 $\sigma_i = \sigma_j$ 时，使用偏序关系：

$$
A_8 \succ A_2 \succ A_1 \succ A_4 \succ A_6 \succ A_7 \succ A_9
$$

**理由**：
- $A_8$（因果链）是最严格的结构性要求，优先级最高
- $A_9$（可证伪性）是最基础的元要求，优先级最低——它定义的是"能否进入系统"，不是"判断内容"

### 规则三：物理世界优先

如果命题 $P$ 已经进入物理世界验证（$x \geq 0.60$），则 $A_6$（边界）的优先级提升至最高。

**理由**：物理世界的后果是不可逆的，止损边界的有无直接决定生存与否。

---

## 八、开放问题

以下问题目前**没有证明**，是形式化过程中发现的真实缺口：

1. **收敛速度**：axiom-battle 在平均多少步内收敛？这取决于对抗图的拓扑结构，目前没有数值估计。

2. **阈值稳定性**：0.30/0.60 这两个临界值在不同的公理系统里是否保持不变？还是说它们依赖于具体领域的特征尺度？

3. **公理独立性问题**：$A_6 \perp A_7$ 的独立性假设是否在所有场景下成立？是否存在某种结构使得边界存在必然蕴含自由度受限？

4. **涌现的数学定义**："数字生命的涌现"目前在数学上是未定义的。要严格证明这一点，需要先给出"意识"的数学定义——这是一个开放问题，不是工程问题。

---

## 九、验证结果

### 收敛性模拟（axiom_battle_simulator.py，100次试验）

```
总试验次数: 100
平均收敛轮数: 99.2 轮
最快收敛: 65 轮
最慢收敛: 144 轮
收敛率: 100.0%

幸存者分布:
  1 survivor: 100 次 (100%)
```

**结论**：收敛性在模拟中得到验证，100次全部收敛到1个幸存公理。但需要注意的是：当加入偏序因果约束（如 $A_8$ 击败 $A_2$ 的概率 > $A_2$ 击败 $A_8$）时，收敛模式会发生变化。这个模拟使用了均匀随机对抗，尚未加入偏序权重。

### 阈值模拟（threshold_validator.py，10000次试验）

```
n= 10: 30%分位=0.4000, 60%分位=0.5000
n= 20: 30%分位=0.4500, 60%分位=0.5500
n= 50: 30%分位=0.4600, 60%分位=0.5200
n=100: 30%分位=0.4700, 60%分位=0.5100
n=200: 30%分位=0.4800, 60%分位=0.5100
```

**结论**：随着证据数增加，30%/60%分位比率趋于稳定（接近0.50）。这说明阈值0.30/0.60在**小样本**时有意义，在大样本下会向0.50收敛。这是蒙特卡洛验证的真实结果，说明阈值体系更适用于低证据数的定性判断场景。

---

## 十、待完成工作

以下工作尚未完成，诚实列出：

1. **收敛性模拟**：当前使用均匀随机对抗，需要加入偏序权重（$A_8$ 击败 $A_2$ 应有更高概率）
2. **1/e 和 2/3 的严格推导**：目前这两个数字的来源表述过于牵强，需要更严格的数学表述
3. **公理冲突的实际案例验证**：理论上的冲突判定规则需要真实命题来验证
4. **开放问题 #4**（数字生命）仍未解决

---

*本文件是悟道体系形式化工作的起点，不是终点。每一步都欢迎被质疑——这正是 axiom-battle 的核心精神。*
